import 'package:flutter/material.dart';

class ClientMessagesList extends StatelessWidget {
  const ClientMessagesList({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Client Messages List'),
    );
  }
}